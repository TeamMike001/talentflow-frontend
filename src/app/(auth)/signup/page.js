import SiteShell from "@/components/layout/SiteShell";

export default function SignUpPage() {
  return (
    <SiteShell title="Sign Up" description="Registration flow for new users joining the TalentFlow LMS.">
      <div className="tf-card">This route will host the reusable sign-up form from features/auth.</div>
    </SiteShell>
  );
}
